nltk>=3.6.0
numpy>=1.21.0
pandas>=1.3.0
Pillow>=8.2.0
beautifulsoup4>=4.9.0
selenium>=4.0.0
selenium-stealth>=1.0.0
langchain_community>=0.0.0
IPython>=7.0.0
torch>=1.10.0              # For model training and fine-tuning
transformers>=4.17.0       # For using Hugging Face's models
concurrent.futures>=3.0.0  # For handling multithreading (Python 3.2+ has this by default, but older versions might require it)
jsonpickle>=2.0.0          # For serializing objects like models
datasets>=1.18.0           # For handling datasets